# Configuration des scripts d'installation (Scripts_serveur)
# Copier vers .env et adapter. Les scripts chargent ce fichier automatiquement
# s'il est présent à la racine du projet (à côté de install.sh).

# Dossier racine pour mots de passe, configs, règles iptables, logs, backups.
# Défaut : /root/Devops
DEVOPS_ROOT=/root/Devops

# Optionnel : email admin (utilisé par MOTD, vhost, fail2ban si non défini ailleurs)
# ADMIN_EMAIL=admin@example.com

# Optionnel : nombre de jours de rétention des logs (logrotate, défaut selon script)
# LOG_ROTATE_DAYS=14
